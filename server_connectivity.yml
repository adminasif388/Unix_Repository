---
- name: Server Connectivity Check and Save Report
  hosts: all
  become: true
  tasks:
    - name: Ping tasks
      ansible.builtin.ping:

- name: Send email result
      delegate_to: vm01
      mail:
        host: smtp.gmail.com
        port: 25
        to: asifali388@gmail.com
        from: nazmabatool388@gmail.com
        subject: "Server {{ inventory_hostname }} Status"
        body: |
          Host: {{ inventory_hostname }}
          Status: {{ 'SUCCESS' if result is succeeded else 'FAILED' }}
      
